spring:
  application:
    name: friendrequest-service
  datasource:
    url: jdbc:mysql://localhost:3306/userDB
    username: root
    password: AaYyUuSsHh123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
